import { Box, Stack, styled } from "@mui/material";

export const FrameBox = styled(Stack)(({theme})=>({
  // width: "715px",
  // height: "768px",
  // border: "1px solid rgba(191, 149, 63, 0.51)",
  // backgroundColor: "#f5efe2",
  // zIndex: -1,
}));
